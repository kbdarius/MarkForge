<UserControl x:Class="MarkForge.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="24" FontWeight="SemiBold" Text="Settings" />

        <TextBlock Grid.Row="1"
                   Margin="0,16,0,6"
                   FontWeight="SemiBold"
                   Text="Settings file location" />
        <TextBox Grid.Row="2" IsReadOnly="True" Text="{Binding SettingsFilePath}" />

        <GroupBox Grid.Row="3" Margin="0,16,0,0" Padding="12" Header="Saved Settings Snapshot">
            <DockPanel>
                <Button DockPanel.Dock="Top"
                        Width="150"
                        Height="32"
                        Margin="0,0,0,10"
                        Command="{Binding RefreshCommand}"
                        Content="Refresh Snapshot" />
                <TextBox IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         FontFamily="Consolas"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding SettingsSummary}" />
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
